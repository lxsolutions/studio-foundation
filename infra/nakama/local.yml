# Nakama development config. Production configuration lives outside this repo.
name: studio-foundation
data_dir: /nakama/data

logger:
  level: INFO

runtime:
  js_entrypoint: index.js

socket:
  server_key: defaultkey # development only; override outside local environments
