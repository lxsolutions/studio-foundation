//! Intentionally empty — this crate exists for its `tests/` directory.
//! `cargo test -p studio-integration-tests` runs the connectivity suite;
//! DB-backed tests are `#[ignore]` and run via `just test-db`.
